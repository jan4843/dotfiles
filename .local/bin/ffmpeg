#!/bin/sh

exec docker container run \
	--interactive \
	--rm \
	--tty \
	--volume "$HOME:$HOME" \
	--volume "$PWD:$PWD" \
	--workdir "$PWD" \
	--entrypoint "$(basename "$0")" \
	lscr.io/linuxserver/ffmpeg \
	"$@"
